export default function Contact (){
    
    return( 
        <div>
            <ul>
                <button onClick={()=>{window.location.assign('/')}}>home</button>
                <button onClick={()=>{window.location.assign('/contact')}}>home</button>

            </ul>
            <h1>contact page</h1>
        </div>
    )
}